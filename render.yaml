services:
  - type: web
    name: ai-risk-model-app
    env: python
    buildCommand: ""
    startCommand: uvicorn main:app --host=0.0.0.0 --port=10000
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.8
